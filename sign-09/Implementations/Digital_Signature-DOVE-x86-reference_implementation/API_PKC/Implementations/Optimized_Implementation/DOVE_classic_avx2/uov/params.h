/// @file params.h
/// @brief Minimal stub of UOV params.h for DOVE.
///        The only relevant macro is _USE_GF16 (must remain undefined
///        so that the GF(256) path of blas_matrix_ref.c is selected).
///

#ifndef _PARAMS_H_
#define _PARAMS_H_

// Intentionally leave _USE_GF16 undefined to use the GF(256) code path.

#endif // _PARAMS_H_
